#ifndef SERVIDOR_H //Si no esta definida la constante SERVIDOR_H compilalo
    #define SERVIDOR_H //define la constante SERVIDOR_H compilalo
    int iniServidor();
    int Servidor();
#endif // Cerramos el if