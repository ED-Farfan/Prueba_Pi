#ifndef FICHERO_H //Si no esta definida la constante FICHERO_H compilalo
    #define FICHERO_H //define la constante FICHERO_H compilalo
    char*Leer_Fichero(char ruta[],char buscar[]);
#endif // Cerramos el if