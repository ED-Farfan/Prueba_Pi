#ifndef HILO_H //Si no esta definida la constante HILO_H compilalo
    #define HILO_H //define la constante HILO_H compilalo
    #include "defs.h"
    void* funHilo(void *arg);
    void Hilo(GPGGA*dato);
#endif // Cerramos el if